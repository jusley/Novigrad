<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.novigrad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".employeeprofile"></activity>
        <activity android:name=".emplog" />
        <activity android:name=".clientlog" />
        <activity android:name=".clientprofile" />
        <activity android:name=".Register" />
        <activity android:name=".loginGeneral" />
        <activity
            android:name=".ui.login.loginGeneral"
            android:label="@string/title_activity_login_general" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>